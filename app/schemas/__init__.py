"""The frozen §6 API contract.

This package is the contract. After Phase 0 it does not change without an
explicit, announced decision, because a mobile client is built against it.
A diff that touches this directory is a contract change by definition.
"""
