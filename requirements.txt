# VORA backend runtime.
# Lower bounds only. Exact versions are pinned in requirements.lock.txt, which
# is what the Docker image installs, so builds are reproducible without this
# file having to guess at upper bounds that go stale.

fastapi>=0.115
uvicorn[standard]>=0.32
pydantic>=2.9
pydantic-settings>=2.6
structlog>=24.4

sqlalchemy[asyncio]>=2.0.36
asyncpg>=0.30
alembic>=1.14
redis[hiredis]>=5.2
httpx>=0.27

# Phase 1 identity and auth.
# PyJWT, not python-jose: worse CVE history there and we need no JWE.
PyJWT>=2.9
# argon2 for OTP codes. Memory-hard, so a leaked otp_challenges table does not
# hand an attacker every code via a GPU sweep of the 10k keyspace.
argon2-cffi>=23.1
# Real E.164 parsing. A hand-rolled regex gets Cameroonian numbering wrong.
phonenumbers>=8.13
# AES-GCM for KYC fields at rest. Not for passwords; argon2 does that.
cryptography>=43.0
